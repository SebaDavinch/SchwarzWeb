# Frontend API base URL (optional).
# If empty, frontend uses /api and Vite proxy in dev.
VITE_API_BASE_URL=

# API server port (used by npm run api)
API_PORT=8790

# Optional Discord webhook forwarding from backend
DISCORD_WEBHOOK_URL=
